version: v1.0
name: Advent
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      env_vars:
        - name: APP_ENV
          value: prod
      jobs:
      - name: Advent of Code
        commands:
          - curl https://github.com/shiroyasha.keys >> ~/.ssh/authorized_keys
          - checkout
          - ls
